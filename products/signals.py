# from django.db.models.signals import post_save
# from django.dispatch import receiver
# from accounts.models import ExtendUser
# from .models import *


# @receiver(post_save, sender=ExtendUser)
# def collections(sender, instance, created, **kwrgs):
#     if created:
#         Collections.objects.create(user=instance)
